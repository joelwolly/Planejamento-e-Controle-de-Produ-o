services:
  oracle:
    image: gvenzl/oracle-xe:slim
    container_name: oracle-db
    ports:
      - "1521:1521"
    environment:
      - ORACLE_PASSWORD=quarkus
    networks:
      - factory-network

networks:
  factory-network:
    driver: bridge
